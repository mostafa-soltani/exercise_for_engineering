arr = [4,1,2,1,2,3,5,3,5]

seen = {}

for every index from 0 to len(arr) - 1:
    if arr[index] is not in seen:
        add arr[index] to seen with count 1

    else:
        change the arr[index] in seen and add 1 to count

for item,count in seen.items():
    if count == 1:
        return item