this prblem wants to find a key in a sorted array that its cut and break in half of array that when we find the mid one of halfs is sorted


def binary(arr,key):

    while low <= high:
        mid = low + (high - low) // 2

        if arr[low] < arr[mid]:
            if arr[low] == key:
                return low

            else:
                low = low + 1

         
    while mid <= high:
        mid = mid +(high - mid) // 2

        if arr[mid] == key:
            return key

        else:
            mid = mid +1